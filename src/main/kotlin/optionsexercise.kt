fun main() {
    var tennum = mutableListOf(1, 2, 3, 4, 5,11,7,8,9,10,6)
  var bigist = Int-> Int

        for (index in 0 until tennum.size) {
            var bignumber = 0
            if (tennum[index] > bignumber) {
                bignumber == tennum[index]
            }
            println(bigist)
    }
  println(bigist)
}
